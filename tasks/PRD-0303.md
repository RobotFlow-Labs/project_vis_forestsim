# Task PRD-0303: Add Overlay Rendering

> PRD: PRD-03 | Depends: PRD-0302  
> Files: 2 | Lines: ~130 | Time: ~45min

## What to Build

Implement palette-based colorization and alpha overlays suitable for reproducing paper-style qualitative outputs.

## Files

### Create: `src/anima_vis_forestsim/infer/overlay.py`
```python
def colorize(mask: np.ndarray, palette: list[tuple[int, int, int]]) -> np.ndarray:
    ...
```

### Create: `tests/test_overlay.py`
```python
def test_colorize_returns_rgb():
    assert colorize(mask, palette).shape[-1] == 3
```

## Verify
```bash
uv run pytest tests/test_overlay.py -v
```
