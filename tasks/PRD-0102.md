# Task PRD-0102: Add Typed Settings And Path Resolution

> PRD: PRD-01 | Depends: PRD-0101  
> Files: 2 | Lines: ~170 | Time: ~60min

## What to Build

Implement typed runtime settings for dataset roots, checkpoint roots, default ontology, and compatibility aliases used by the reference repo.

## Files

### Create: `src/anima_vis_forestsim/config.py`
```python
class ForestSimSettings(BaseSettings):
    dataset_root: Path
    checkpoint_root: Path
    default_ontology: str = "forestsim24"
```

### Create: `tests/test_config.py`
```python
def test_settings_accept_alias_paths():
    settings = ForestSimSettings(dataset_root="/tmp/data", checkpoint_root="/tmp/ckpts")
    assert settings.default_ontology == "forestsim24"
```

## Verify
```bash
uv run pytest tests/test_config.py -v
```
