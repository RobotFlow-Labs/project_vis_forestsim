# Task PRD-0301: Implement Checkpoint Resolver

> PRD: PRD-03 | Depends: PRD-0204  
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build

Implement checkpoint lookup and validation so predictor code can resolve a model ID into a local or downloadable weight artifact.

## Files

### Create: `src/anima_vis_forestsim/models/checkpoints.py`
```python
def resolve_checkpoint(model_id: str, root: Path) -> Path:
    return root / f"{model_id}.pth"
```

### Create: `tests/test_checkpoints.py`
```python
def test_checkpoint_path_has_pth_suffix(tmp_path):
    assert resolve_checkpoint("m12", tmp_path).suffix == ".pth"
```

## Verify
```bash
uv run pytest tests/test_checkpoints.py -v
```
