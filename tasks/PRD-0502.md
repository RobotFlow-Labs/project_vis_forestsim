# Task PRD-0502: Implement FastAPI Inference Service

> PRD: PRD-05 | Depends: PRD-0501  
> Files: 2 | Lines: ~180 | Time: ~60min

## What to Build

Implement FastAPI endpoints that wrap the predictor and return segmentation outputs.

## Files

### Create: `src/anima_vis_forestsim/api/app.py`
```python
app = FastAPI()

@app.get("/health")
def health():
    return {"ok": True}
```

### Create: `tests/test_api.py`
```python
def test_health_endpoint(client):
    assert client.get("/health").status_code == 200
```

## Verify
```bash
uv run pytest tests/test_api.py -v
```
