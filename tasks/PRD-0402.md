# Task PRD-0402: Reproduce Split And Dataset Sweep

> PRD: PRD-04 | Depends: PRD-0401  
> Files: 2 | Lines: ~180 | Time: ~60min

## What to Build

Create the evaluation runner that iterates over the test split and aggregates metrics across the dataset.

## Files

### Create: `src/anima_vis_forestsim/eval/runner.py`
```python
def run_eval(predictor, split: list[str]) -> dict[str, float]:
    ...
```

### Create: `tests/test_eval_runner.py`
```python
def test_eval_runner_returns_summary():
    assert "aAcc" in run_eval(predictor, split)
```

## Verify
```bash
uv run pytest tests/test_eval_runner.py -v
```
