# Task PRD-0202: Define Benchmark Family Specs

> PRD: PRD-02 | Depends: PRD-0201  
> Files: 2 | Lines: ~170 | Time: ~60min

## What to Build

Define dataclasses for benchmark families so the ANIMA code can resolve architecture family, scheduler, optimizer, and ontology independently from the repo’s raw config files.

## Files

### Create: `src/anima_vis_forestsim/models/families.py`
```python
@dataclass
class BenchmarkFamily:
    family: str
    optimizer: str
    max_iters: int
```

### Create: `tests/test_family_specs.py`
```python
def test_mask2former_family_uses_160k_iters():
    assert FAMILY_SPECS["mask2former"].max_iters == 160000
```

## Verify
```bash
uv run pytest tests/test_family_specs.py -v
```
