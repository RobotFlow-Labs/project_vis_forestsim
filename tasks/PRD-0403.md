# Task PRD-0403: Generate Table I Comparison Report

> PRD: PRD-04 | Depends: PRD-0402  
> Files: 2 | Lines: ~170 | Time: ~60min

## What to Build

Generate a markdown report that aligns observed metrics with the published Table I targets and computes deltas.

## Files

### Create: `src/anima_vis_forestsim/eval/report.py`
```python
def render_table_i_report(observed: dict, expected: dict) -> str:
    ...
```

### Create: `tests/test_table_i_report.py`
```python
def test_report_mentions_model_id():
    assert "m12" in render_table_i_report(observed, expected)
```

## Verify
```bash
uv run pytest tests/test_table_i_report.py -v
```
