# Task PRD-0203: Emit Corrected Config Bundles

> PRD: PRD-02 | Depends: PRD-0202  
> Files: 2 | Lines: ~220 | Time: ~75min

## What to Build

Generate corrected config payloads that patch invalid class counts from the repo and bind the selected ontology and split paths.

## Files

### Create: `src/anima_vis_forestsim/models/config_emit.py`
```python
def emit_config(model_id: str, ontology: OntologySpec) -> dict:
    spec = resolve_spec(model_id)
    return {"num_classes": ontology.num_classes, "max_iters": spec.max_iters}
```

### Create: `tests/test_benchmark_configs.py`
```python
def test_emitted_config_uses_ontology_class_count():
    cfg = emit_config("m12", FORESTSIM24)
    assert cfg["num_classes"] == 24
```

## Verify
```bash
uv run pytest tests/test_benchmark_configs.py -v
```
