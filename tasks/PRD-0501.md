# Task PRD-0501: Define API Schemas

> PRD: PRD-05 | Depends: PRD-0304  
> Files: 2 | Lines: ~110 | Time: ~30min

## What to Build

Define request and response schemas for health, metadata, and segmentation endpoints.

## Files

### Create: `src/anima_vis_forestsim/api/schemas.py`
```python
class SegmentResponse(BaseModel):
    checkpoint_id: str
    ontology: str
```

### Create: `tests/test_api_schemas.py`
```python
def test_schema_has_checkpoint_id():
    assert "checkpoint_id" in SegmentResponse.model_fields
```

## Verify
```bash
uv run pytest tests/test_api_schemas.py -v
```
