# Task PRD-0304: Ship Inference CLI

> PRD: PRD-03 | Depends: PRD-0303  
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build

Expose a CLI that takes an image path and emits mask PNG, overlay PNG, and JSON metadata for checkpoint and ontology.

## Files

### Create: `src/anima_vis_forestsim/infer/cli.py`
```python
app = typer.Typer()

@app.command()
def run(image_path: Path, checkpoint_id: str = "m12"):
    ...
```

### Create: `tests/test_cli.py`
```python
def test_cli_help(runner):
    result = runner.invoke(app, ["--help"])
    assert result.exit_code == 0
```

## Verify
```bash
uv run pytest tests/test_cli.py -v
```
