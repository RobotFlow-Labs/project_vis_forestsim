# Task PRD-0201: Build Paper Benchmark Registry

> PRD: PRD-02 | Depends: PRD-0104  
> Files: 2 | Lines: ~180 | Time: ~60min

## What to Build

Create the registry of paper models `m1` through `m13`, including family name, backbone, decoder, pretrained source, and target metrics from Table I.

## Files

### Create: `src/anima_vis_forestsim/models/registry.py`
```python
PAPER_TABLE = {
    "m1": {"family": "pspnet", "backbone": "resnet50v1c"},
}
```

### Create: `tests/test_model_registry.py`
```python
def test_registry_contains_all_paper_models():
    assert len(PAPER_TABLE) == 13
```

## Verify
```bash
uv run pytest tests/test_model_registry.py -v
```
