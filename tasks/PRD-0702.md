# Task PRD-0702: Add Runtime Fallback Checks

> PRD: PRD-07 | Depends: PRD-0701  
> Files: 2 | Lines: ~110 | Time: ~30min

## What to Build

Add environment checks and graceful fallback logic for missing CUDA, missing checkpoints, and unsupported ontology names.

## Files

### Create: `src/anima_vis_forestsim/runtime_checks.py`
```python
def validate_runtime(settings) -> list[str]:
    ...
```

### Create: `tests/test_runtime_fallbacks.py`
```python
def test_missing_checkpoint_returns_warning():
    ...
```

## Verify
```bash
uv run pytest tests/test_runtime_fallbacks.py -v
```
