# Task PRD-0401: Implement Metric Primitives

> PRD: PRD-04 | Depends: PRD-0304  
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build

Implement confusion-matrix, IoU, per-class accuracy, and aggregate metric functions aligned with the paper’s `aAcc`, `mIoU`, and `mAcc`.

## Files

### Create: `src/anima_vis_forestsim/eval/metrics.py`
```python
def summarize_metrics(confusion: np.ndarray) -> dict[str, float]:
    ...
```

### Create: `tests/test_metrics.py`
```python
def test_metrics_include_miou():
    summary = summarize_metrics(confusion)
    assert "mIoU" in summary
```

## Verify
```bash
uv run pytest tests/test_metrics.py -v
```
