# Task PRD-0103: Encode ForestSim Ontologies

> PRD: PRD-01 | Depends: PRD-0102  
> Files: 2 | Lines: ~220 | Time: ~75min

## What to Build

Encode the 24-class palette from the repo, the 6-class grouped traversability ontology, and the alias logic that maps paper/repo names onto those specs.

## Files

### Create: `src/anima_vis_forestsim/data/ontology.py`
```python
FORESTSIM24 = OntologySpec(name="forestsim24", num_classes=24, palette=[...])
FORESTSIM6 = OntologySpec(name="forestsim6", num_classes=6, palette=[...])
```

### Create: `tests/test_ontology.py`
```python
def test_vail_all_alias_resolves_to_forestsim24():
    assert resolve_ontology("vail_all").name == "forestsim24"
```

## Verify
```bash
uv run pytest tests/test_ontology.py -v
```
