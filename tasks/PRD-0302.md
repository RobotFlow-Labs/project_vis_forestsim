# Task PRD-0302: Build Predictor Core

> PRD: PRD-03 | Depends: PRD-0301  
> Files: 2 | Lines: ~220 | Time: ~90min

## What to Build

Build the predictor class that preprocesses RGB images, invokes the resolved benchmark model, and returns logits plus argmax masks.

## Files

### Create: `src/anima_vis_forestsim/infer/predictor.py`
```python
class ForestSimPredictor:
    def predict(self, image: np.ndarray, checkpoint_id: str, ontology: str):
        ...
```

### Create: `tests/test_predictor.py`
```python
def test_predictor_returns_mask_shape():
    assert mask.shape[-2:] == (512, 512)
```

## Verify
```bash
uv run pytest tests/test_predictor.py -v
```
