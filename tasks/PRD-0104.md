# Task PRD-0104: Capture Asset Manifest And Drift Report

> PRD: PRD-01 | Depends: PRD-0103  
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build

Implement the runtime asset manifest model and generate a local drift report covering class counts, split layouts, and invalid `num_classes` settings in the repo configs.

## Files

### Create: `src/anima_vis_forestsim/data/manifest.py`
```python
@dataclass
class AssetManifest:
    datasets: list[dict]
    checkpoints: list[dict]
```

### Create: `tests/test_manifest.py`
```python
def test_manifest_serializes():
    manifest = AssetManifest(datasets=[], checkpoints=[])
    assert manifest.datasets == []
```

## Verify
```bash
uv run pytest tests/test_manifest.py -v
```
